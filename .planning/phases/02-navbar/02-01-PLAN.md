---
phase: 02-navbar
plan: 01
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [
  "src/components/shared/Navbar.tsx",
  "src/components/shared/MobileMenu.tsx",
  "src/components/shared/NavDropdown.tsx",
  "src/constants/nav.ts"
]
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]

must_haves:
  truths:
    - "Navbar is fixed at top with transparent → glass blur transition on scroll"
    - "Navigation links have hover underline animation"
    - "Services and For Employers dropdowns open on hover with slide-down"
    - "Mobile hamburger opens drawer from right"
  artifacts:
    - path: "src/components/shared/Navbar.tsx"
      provides: "Fixed navbar with scroll-aware glass effect"
      exports: ["Navbar"]
      min_lines: 80
    - path: "src/components/shared/NavDropdown.tsx"
      provides: "Animated dropdown menu"
      exports: ["NavDropdown"]
    - path: "src/components/shared/MobileMenu.tsx"
      provides: "Mobile hamburger drawer"
      exports: ["MobileMenu"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/shared/Navbar.tsx"
      via: "import and render"
    - from: "Navbar.tsx"
      to: "NavDropdown.tsx"
      via: "renders dropdowns for Services and For Employers"
    - from: "Navbar.tsx"
      to: "MobileMenu.tsx"
      via: "renders mobile menu on small screens"
---

<objective>
Build the Navbar component for Hiretivo — exact replica of antimatterai.com navbar with scroll-aware glass blur, hover dropdowns, and mobile hamburger drawer.

Purpose: Primary navigation for the entire site, establishing the header pattern.
Output: Fixed navbar with all interactions working (scroll blur, dropdowns, mobile menu).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/phases/01-setup/01-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

From src/app/layout.tsx (from Phase 1):
```tsx
<html lang="en" className={`${playfair.variable} ${dmSans.variable} ${jetbrains.variable}`}>
  <body className="bg-black text-white">
    <CustomCursor />
    {children}
  </body>
</html>
```

From tailwind.config.ts:
- fontFamily: { display: ["var(--font-playfair)", "serif"], body: ["var(--font-dm-sans)", "sans-serif"], mono: ["var(--font-jetbrains)", "monospace"] }
- colors: { background: { primary: "#000000", secondary: "#0a0a0a" }, foreground: { primary: "#ffffff", secondary: "#a0a0a0", muted: "#555555" }, border: { subtle: "rgba(255,255,255,0.08)" } }

From PROJECT.md:
- Nav: glass blur on scroll (`backdrop-filter: blur(12px)`)
- CTA button: subtle scale 1.02 on hover + shimmer effect
- No barrel imports
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NavDropdown component</name>
  <files>src/components/shared/NavDropdown.tsx, src/constants/nav.ts</files>
  <action>
    1. Create src/constants/nav.ts with navigation data:
    ```typescript
    export const SERVICES_ITEMS = [
      {
        title: "Product Design",
        href: "/design",
        items: ["User Research", "UX Flows", "UI Systems", "Dev Handoff"],
      },
      {
        title: "AI Matching Engine",
        href: "/matching",
        items: ["Skill-Overlap Algorithm", "Match Score", "Ranked Feed", "Bias Filtering"],
      },
      {
        title: "Employer Tools",
        href: "/employers",
        items: ["Job Post Management", "ATS Integration", "Analytics", "Screening"],
      },
      {
        title: "Candidate Tools",
        href: "/candidates",
        items: ["Profile Builder", "Skill Tags", "Job Feed", "Applications"],
      },
    ] as const;

    export const EMPLOYER_ITEMS = [
      { title: "Starter Plan", href: "/pricing#starter" },
      { title: "Growth Plan", href: "/pricing#growth" },
      { title: "Enterprise", href: "/pricing#enterprise" },
    ] as const;

    export const NAV_LINKS = [
      { label: "Work", href: "/work" },
      { label: "Company", href: "/company" },
      { label: "Services", href: "#services", hasDropdown: true },
      { label: "For Employers", href: "#employers", hasDropdown: true },
    ] as const;
    ```

    2. Create src/components/shared/NavDropdown.tsx:
    ```tsx
    "use client";

    import { motion, AnimatePresence } from "framer-motion";
    import { useState } from "react";
    import Link from "next/link";

    interface DropdownItem {
      title: string;
      href: string;
      items?: string[];
    }

    interface NavDropdownProps {
      label: string;
      items: DropdownItem[];
    }

    export function NavDropdown({ label, items }: NavDropdownProps) {
      const [isOpen, setIsOpen] = useState(false);

      return (
        <div
          className="relative"
          onMouseEnter={() => setIsOpen(true)}
          onMouseLeave={() => setIsOpen(false)}
        >
          <span className="cursor-pointer text-sm font-medium text-white/80 hover:text-white transition-colors">
            {label}
          </span>
          <AnimatePresence>
            {isOpen && (
              <motion.div
                initial={{ opacity: 0, y: -8 }}
                animate={{ opacity: 1, y: 0 }}
                exit={{ opacity: 0, y: -8 }}
                transition={{ duration: 0.2, ease: "easeOut" }}
                className="absolute top-full left-0 mt-2 w-72 bg-[#0a0a0a] border border-white/[0.08] rounded-xl p-4 shadow-2xl"
              >
                {items.map((item) => (
                  <Link
                    key={item.title}
                    href={item.href}
                    className="block p-3 rounded-lg hover:bg-white/[0.03] transition-colors group"
                  >
                    <div className="text-sm font-medium text-white group-hover:text-white">
                      {item.title}
                    </div>
                    {item.items && (
                      <div className="mt-1 text-xs text-[#555555]">
                        {item.items.join(" · ")}
                      </div>
                    )}
                  </Link>
                ))}
              </motion.div>
            )}
          </AnimatePresence>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "NavDropdown" src/components/shared/NavDropdown.tsx && grep -n "SERVICES_ITEMS" src/constants/nav.ts</automated>
  </verify>
  <done>NavDropdown component renders dropdown with items, hover trigger, slide-down animation.</done>
</task>

<task type="auto">
  <name>Task 2: Create Navbar component with scroll blur</name>
  <files>src/components/shared/Navbar.tsx</files>
  <action>
    Create src/components/shared/Navbar.tsx:
    ```tsx
    "use client";

    import { useState } from "react";
    import { motion, useScroll, useMotionValueEvent } from "framer-motion";
    import Link from "next/link";
    import { NavDropdown } from "./NavDropdown";
    import { MobileMenu } from "./MobileMenu";
    import { NAV_LINKS, SERVICES_ITEMS, EMPLOYER_ITEMS } from "@/constants/nav";

    export function Navbar() {
      const [isScrolled, setIsScrolled] = useState(false);
      const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);
      const { scrollY } = useScroll();

      useMotionValueEvent(scrollY, "change", (latest) => {
        setIsScrolled(latest > 50);
      });

      return (
        <>
          <motion.header
            initial={{ y: -100 }}
            animate={{ y: 0 }}
            transition={{ duration: 0.6, ease: "easeOut" }}
            className={`fixed top-0 left-0 right-0 z-50 transition-all duration-300 ${
              isScrolled
                ? "bg-black/80 backdrop-blur-xl border-b border-white/[0.08]"
                : "bg-transparent"
            }`}
          >
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
              <nav className="flex items-center justify-between h-16 lg:h-20">
                {/* Logo */}
                <Link
                  href="/"
                  className="font-[family-name:var(--font-playfair)] text-xl text-white"
                >
                  Hiretivo
                </Link>

                {/* Desktop Navigation */}
                <div className="hidden lg:flex items-center gap-8">
                  {NAV_LINKS.map((link) =>
                    link.hasDropdown ? (
                      <NavDropdown
                        key={link.label}
                        label={link.label}
                        items={
                          link.label === "Services"
                            ? SERVICES_ITEMS
                            : EMPLOYER_ITEMS
                        }
                      />
                    ) : (
                      <Link
                        key={link.label}
                        href={link.href}
                        className="relative text-sm font-medium text-white/80 hover:text-white transition-colors group"
                      >
                        {link.label}
                        <span className="absolute -bottom-1 left-0 w-0 h-[1px] bg-white group-hover:w-full transition-all duration-300" />
                      </Link>
                    )
                  )}
                </div>

                {/* CTA Button */}
                <div className="hidden lg:block">
                  <Link
                    href="/post-job"
                    className="inline-flex items-center px-5 py-2 bg-white text-black text-sm font-medium rounded-full hover:scale-[1.02] transition-transform"
                  >
                    Post a Job
                  </Link>
                </div>

                {/* Mobile Hamburger */}
                <button
                  className="lg:hidden relative w-8 h-8 flex flex-col justify-center items-center gap-1.5"
                  onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
                  aria-label="Toggle menu"
                >
                  <motion.span
                    animate={{
                      rotate: isMobileMenuOpen ? 45 : 0,
                      y: isMobileMenuOpen ? 6 : 0,
                    }}
                    className="block w-6 h-[1.5px] bg-white origin-center"
                  />
                  <motion.span
                    animate={{
                      opacity: isMobileMenuOpen ? 0 : 1,
                    }}
                    className="block w-6 h-[1.5px] bg-white"
                  />
                  <motion.span
                    animate={{
                      rotate: isMobileMenuOpen ? -45 : 0,
                      y: isMobileMenuOpen ? -6 : 0,
                    }}
                    className="block w-6 h-[1.5px] bg-white origin-center"
                  />
                </button>
              </nav>
            </div>
          </motion.header>

          {/* Mobile Menu */}
          <MobileMenu
            isOpen={isMobileMenuOpen}
            onClose={() => setIsMobileMenuOpen(false)}
          />
        </>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "useScroll" src/components/shared/Navbar.tsx && grep -n "backdrop-blur" src/components/shared/Navbar.tsx && grep -n "NavDropdown" src/components/shared/Navbar.tsx</automated>
  </verify>
  <done>Navbar has scroll-aware glass blur, logo, nav links with hover underline, dropdowns, CTA button, mobile hamburger with animated icon.</done>
</task>

<task type="auto">
  <name>Task 3: Create MobileMenu component</name>
  <files>src/components/shared/MobileMenu.tsx</files>
  <action>
    Create src/components/shared/MobileMenu.tsx:
    ```tsx
    "use client";

    import { motion, AnimatePresence } from "framer-motion";
    import Link from "next/link";
    import { NAV_LINKS, SERVICES_ITEMS, EMPLOYER_ITEMS } from "@/constants/nav";

    interface MobileMenuProps {
      isOpen: boolean;
      onClose: () => void;
    }

    export function MobileMenu({ isOpen, onClose }: MobileMenuProps) {
      return (
        <AnimatePresence>
          {isOpen && (
            <>
              {/* Backdrop */}
              <motion.div
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                exit={{ opacity: 0 }}
                className="fixed inset-0 bg-black/60 z-40 lg:hidden"
                onClick={onClose}
              />
              {/* Drawer */}
              <motion.div
                initial={{ x: "100%" }}
                animate={{ x: 0 }}
                exit={{ x: "100%" }}
                transition={{ type: "spring", damping: 30, stiffness: 300 }}
                className="fixed top-0 right-0 bottom-0 w-[300px] bg-[#0a0a0a] border-l border-white/[0.08] z-50 lg:hidden"
              >
                <div className="p-6 pt-20">
                  <nav className="flex flex-col gap-6">
                    {NAV_LINKS.map((link) => (
                      <div key={link.label}>
                        {link.hasDropdown ? (
                          <div>
                            <span className="text-lg font-medium text-white">
                              {link.label}
                            </span>
                            <div className="mt-3 ml-4 flex flex-col gap-3">
                              {(link.label === "Services"
                                ? SERVICES_ITEMS
                                : EMPLOYER_ITEMS
                              ).map((item) => (
                                <Link
                                  key={item.title}
                                  href={item.href}
                                  className="text-sm text-white/60 hover:text-white transition-colors"
                                  onClick={onClose}
                                >
                                  {item.title}
                                </Link>
                              ))}
                            </div>
                          </div>
                        ) : (
                          <Link
                            href={link.href}
                            className="text-lg font-medium text-white hover:text-white/80 transition-colors"
                            onClick={onClose}
                          >
                            {link.label}
                          </Link>
                        )}
                      </div>
                    ))}
                    <Link
                      href="/post-job"
                      className="mt-4 inline-flex items-center justify-center px-5 py-3 bg-white text-black text-sm font-medium rounded-full"
                      onClick={onClose}
                    >
                      Post a Job
                    </Link>
                  </nav>
                </div>
              </motion.div>
            </>
          )}
        </AnimatePresence>
      );
    }
    ```

    2. Update layout.tsx to render Navbar (add after `<CustomCursor />`):
    ```tsx
    import { Navbar } from "@/components/shared/Navbar";
    
    // In body:
    <body className="bg-black text-white">
      <CustomCursor />
      <Navbar />
      {children}
    </body>
    ```
  </action>
  <verify>
    <automated>grep -n "MobileMenu" src/components/shared/MobileMenu.tsx && grep -n "Navbar" src/app/layout.tsx && grep -n "x: \"100%\"" src/components/shared/MobileMenu.tsx</automated>
  </verify>
  <done>MobileMenu renders slide-in drawer from right with backdrop, all nav links and CTA, closes on link click or backdrop tap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→DOM | Navigation state managed client-side only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Navbar links | accept | Standard Next.js Link components, no auth |
| T-02-02 | Information Disclosure | Mobile menu state | accept | UI state only, no sensitive data |
</threat_model>

<verification>
- [ ] Navbar is fixed at top
- [ ] Scroll down >50px triggers glass blur + border
- [ ] Nav links have hover underline (scaleX animation)
- [ ] Services dropdown shows 4 items on hover
- [ ] For Employers dropdown shows 3 pricing tiers
- [ ] Dropdown animation: opacity 0→1, y: -8→0
- [ ] CTA button visible on desktop
- [ ] Mobile hamburger visible below lg breakpoint
- [ ] Hamburger icon animates to X when open
- [ ] Mobile drawer slides from right
- [ ] Backdrop click closes drawer
- [ ] No console errors
</verification>

<success_criteria>
- Fixed navbar with scroll-aware backdrop-filter: blur(12px)
- Transparent → rgba(0,0,0,0.8) background transition
- Logo "Hiretivo" in Playfair Display, white, 20px
- Desktop: Work | Company | Services (dropdown) | For Employers (dropdown) | Post a Job CTA
- Dropdown hover trigger with 0.2s slide-down animation
- Mobile hamburger with animated lines → X transition
- Mobile drawer slides from right with spring physics
- Navbar rendered in layout.tsx above page content
</success_criteria>

<output>
After completion, create `.planning/phases/02-navbar/02-01-SUMMARY.md`
</output>
