---
phase: 05-services
plan: 01
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [
  "src/components/home/ServicesSection.tsx",
  "src/components/home/ServiceCard.tsx"
]
autonomous: true
requirements: [SERV-01, SERV-02, SERV-03]

must_haves:
  truths:
    - "Services section shows 6 cards in a 2-column grid"
    - "Each card reveals description and tags on hover"
    - "Cards have border glow and y-translate on hover"
    - "Section heading animates in on scroll"
  artifacts:
    - path: "src/components/home/ServiceCard.tsx"
      provides: "Individual service card with hover reveal"
      exports: ["ServiceCard"]
    - path: "src/components/home/ServicesSection.tsx"
      provides: "Services grid section"
      exports: ["ServicesSection"]
  key_links:
    - from: "ServicesSection.tsx"
      to: "ServiceCard.tsx"
      via: "maps services array to cards"
    - from: "src/app/page.tsx"
      to: "ServicesSection.tsx"
      via: "import and render after StatsBar"
---

<objective>
Build the Services Section with 6 hover-reveal cards showing Hiretivo's offerings.

Purpose: Showcase platform features with interactive hover states.
Output: 6-card grid with number labels, titles, descriptions, and tags that reveal on hover.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/phases/01-setup/01-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

From PROJECT.md:
- Section heading: "Our Services" split styling ("Our" muted, "Services" white)
- 6 cards in 2-column grid (desktop), 1-column (mobile)
- Card: black bg, border, rounded-2xl, p-8
- Hover: border brightens, bg rgba(255,255,255,0.03), y: -4px
- Description slides up on hover with tags and arrow
- Scroll: staggered fade-up per card
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServiceCard component</name>
  <files>src/components/home/ServiceCard.tsx</files>
  <action>
    Create src/components/home/ServiceCard.tsx:
    ```tsx
    "use client";

    import { motion } from "framer-motion";
    import { ArrowUpRight } from "lucide-react";

    interface ServiceCardProps {
      number: string;
      title: string;
      description: string;
      tags: string[];
      index: number;
    }

    export function ServiceCard({
      number,
      title,
      description,
      tags,
      index,
    }: ServiceCardProps) {
      return (
        <motion.div
          initial={{ opacity: 0, y: 40 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true, margin: "-100px" }}
          transition={{ duration: 0.5, delay: index * 0.1 }}
          whileHover={{ y: -4 }}
          className="group relative bg-black border border-white/[0.08] rounded-2xl p-8 cursor-pointer transition-colors duration-300 hover:border-white/[0.2] hover:bg-white/[0.03]"
        >
          {/* Number label */}
          <div className="font-[family-name:var(--font-jetbrains)] text-xs text-[#555555] mb-4">
            {number}
          </div>

          {/* Title */}
          <h3 className="font-[family-name:var(--font-playfair)] text-[28px] text-white mb-4 leading-tight">
            {title}
          </h3>

          {/* Description - reveals on hover */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileHover={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.3 }}
            className="overflow-hidden"
          >
            <p className="font-[family-name:var(--font-dm-sans)] text-sm text-[#a0a0a0] leading-relaxed mb-4">
              {description}
            </p>

            {/* Tags */}
            <div className="flex flex-wrap gap-2 mb-4">
              {tags.map((tag) => (
                <span
                  key={tag}
                  className="inline-flex items-center px-3 py-1 text-xs font-medium text-[#a0a0a0] border border-white/[0.1] rounded-full"
                >
                  {tag}
                </span>
              ))}
            </div>
          </motion.div>

          {/* Arrow - appears on hover */}
          <div className="absolute top-8 right-8 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
            <ArrowUpRight className="w-5 h-5 text-white" />
          </div>
        </motion.div>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "ServiceCard" src/components/home/ServiceCard.tsx && grep -n "whileHover" src/components/home/ServiceCard.tsx && grep -n "ArrowUpRight" src/components/home/ServiceCard.tsx</automated>
  </verify>
  <done>ServiceCard renders number, title, hover-reveal description, tags, and arrow icon.</done>
</task>

<task type="auto">
  <name>Task 2: Create ServicesSection with data</name>
  <files>src/components/home/ServicesSection.tsx, src/app/page.tsx</files>
  <action>
    1. Create src/components/home/ServicesSection.tsx:
    ```tsx
    "use client";

    import { motion } from "framer-motion";
    import { ServiceCard } from "./ServiceCard";

    const services = [
      {
        number: "01",
        title: "Skill-Overlap Matching",
        description:
          "Our proprietary engine compares your skill profile against job requirements and ranks matches by overlap score — zero noise, only fits.",
        tags: ["Skill Graph", "Match Score", "Ranked Feed", "Bias-Free"],
      },
      {
        number: "02",
        title: "Smart Job Feed",
        description:
          "A personalized job feed that only shows roles where your skills actually qualify you. No more spray-and-pray applications.",
        tags: ["Personalized Feed", "Relevance Filter", "Real-time Updates", "Saved Searches"],
      },
      {
        number: "03",
        title: "Employer Job Posting",
        description:
          "Post roles with precise skill requirements. Our system automatically routes them to the most qualified candidates.",
        tags: ["Skill Requirements", "ATS Integration", "Candidate Ranking", "Analytics"],
      },
      {
        number: "04",
        title: "Candidate Profile Builder",
        description:
          "Build a rich skill profile with verified skills, experience, and preferences. Your profile does the matching work for you.",
        tags: ["Skill Tags", "Experience Map", "Preference Settings", "Profile Score"],
      },
      {
        number: "05",
        title: "ATS Integrations",
        description:
          "Connect with Greenhouse, Lever, Workable, and Ashby. Sync job postings and applicants without leaving your existing workflow.",
        tags: ["Greenhouse", "Lever", "Workable", "Ashby"],
      },
      {
        number: "06",
        title: "Remote-First Infrastructure",
        description:
          "Every feature built for async, global, timezone-aware remote hiring — from application to offer.",
        tags: ["Remote-First", "Async Tools", "Global Reach", "Timezone Aware"],
      },
    ];

    export function ServicesSection() {
      return (
        <section id="services" className="py-24 lg:py-32 bg-black">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            {/* Section Header */}
            <motion.div
              initial={{ opacity: 0, y: 40 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true, margin: "-100px" }}
              transition={{ duration: 0.6 }}
              className="mb-16"
            >
              <h2 className="font-[family-name:var(--font-playfair)] text-[52px] leading-tight">
                <span className="text-[#555555]">Our</span>{" "}
                <span className="text-white">Services</span>
              </h2>
              <p className="mt-4 font-[family-name:var(--font-dm-sans)] text-lg text-[#a0a0a0] max-w-xl">
                Everything you need to find, match, and hire top talent — powered by
                skill intelligence.
              </p>
            </motion.div>

            {/* Cards Grid */}
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              {services.map((service, index) => (
                <ServiceCard
                  key={service.number}
                  number={service.number}
                  title={service.title}
                  description={service.description}
                  tags={service.tags}
                  index={index}
                />
              ))}
            </div>
          </div>
        </section>
      );
    }
    ```

    2. Update page.tsx to add ServicesSection:
    ```tsx
    import { ServicesSection } from "@/components/home/ServicesSection";
    
    // After StatsBar:
    <ServicesSection />
    ```
  </action>
  <verify>
    <automated>grep -n "ServicesSection" src/app/page.tsx && grep -n "ServiceCard" src/components/home/ServicesSection.tsx && grep -n "grid-cols-2" src/components/home/ServicesSection.tsx</automated>
  </verify>
  <done>ServicesSection renders heading with split styling, subtext, and 6 ServiceCards in 2-column grid with staggered scroll animations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→DOM | Purely presentational |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | ServiceCard hover | accept | CSS transitions, minimal impact |
</threat_model>

<verification>
- [ ] Section heading shows "Our" in muted, "Services" in white
- [ ] Subheading text below heading
- [ ] 6 cards in 2-column grid on desktop
- [ ] Cards stack to 1 column on mobile
- [ ] Each card has number label (01-06)
- [ ] Card title in Playfair Display, 28px
- [ ] Hover: card lifts y: -4px
- [ ] Hover: border brightens to white/[0.2]
- [ ] Hover: description text appears
- [ ] Hover: tags appear as pill badges
- [ ] Hover: arrow icon appears top-right
- [ ] Cards stagger in on scroll (0.1s delay each)
- [ ] No console errors
</verification>

<success_criteria>
- Section heading: "Our" (#555555) + "Services" (white), Playfair Display, 52px
- Subheading: DM Sans, 18px, #a0a0a0, max-width-xl
- 6 cards in 2-column grid (md breakpoint), gap-6
- Card: black bg, border white/[0.08], rounded-2xl, p-8
- Number label: JetBrains Mono, 12px, #555555
- Title: Playfair Display, 28px, white
- Hover: y: -4px, border white/[0.2], bg white/[0.03]
- Description: slides up on hover, DM Sans, 14px, #a0a0a0
- Tags: pill badges, border, rounded-full, text-xs, #a0a0a0
- Arrow: ArrowUpRight from lucide-react, appears on hover
- Staggered scroll reveal: delay index * 0.1s per card
- ServicesSection rendered in page.tsx after StatsBar
</success_criteria>

<output>
After completion, create `.planning/phases/05-services/05-01-SUMMARY.md`
</output>
