---
phase: 07-trustedby
plan: 01
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [
  "src/components/home/TrustedBySection.tsx"
]
autonomous: true
requirements: [TRUST-01, TRUST-02]

must_haves:
  truths:
    - "Trusted By section shows label, heading, and infinite logo ticker"
    - "Company pills scroll left continuously"
    - "Ticker pauses on hover"
    - "Edge fade masks on left and right"
  artifacts:
    - path: "src/components/home/TrustedBySection.tsx"
      provides: "Trusted by section with infinite ticker"
      exports: ["TrustedBySection"]
      min_lines: 80
  key_links:
    - from: "src/app/page.tsx"
      to: "TrustedBySection.tsx"
      via: "import and render after ShowcaseSection"
---

<objective>
Build the "Trusted By" section with infinite horizontal logo ticker using company name pills.

Purpose: Social proof showing companies that use/trust Hiretivo.
Output: Section with heading and seamless infinite-scrolling ticker.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/phases/01-setup/01-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

From PROJECT.md:
- Label: "TRUSTED BY INDUSTRY LEADERS" — JetBrains Mono, 11px, #555555
- Heading: "Powering Remote Hiring for Companies Worldwide" — Playfair Display, 36px
- Infinite horizontal ticker with company pills
- CSS animation: translateX(0) → translateX(-50%), 30s linear infinite
- Hover: animation-play-state: paused
- Pills: border, rounded-full, px-5 py-2
- Edge fade masks using mask-image
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrustedBySection with infinite ticker</name>
  <files>src/components/home/TrustedBySection.tsx, src/app/page.tsx</files>
  <action>
    1. Create src/components/home/TrustedBySection.tsx:
    ```tsx
    "use client";

    import { motion } from "framer-motion";

    const companies = [
      "Vercel",
      "Linear",
      "Notion",
      "Figma",
      "Loom",
      "Raycast",
      "Retool",
      "Supabase",
      "PlanetScale",
      "Fly.io",
      "Resend",
      "Stripe",
    ];

    export function TrustedBySection() {
      // Duplicate for seamless loop
      const tickerItems = [...companies, ...companies];

      return (
        <section className="py-24 lg:py-32 bg-black relative overflow-hidden">
          {/* Background glow */}
          <div
            className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[400px] pointer-events-none"
            style={{
              background:
                "radial-gradient(ellipse at center, rgba(255,255,255,0.05) 0%, transparent 70%)",
            }}
          />

          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
            {/* Label */}
            <motion.p
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5 }}
              className="text-center font-[family-name:var(--font-jetbrains)] text-[11px] uppercase tracking-[0.3em] text-[#555555] mb-6"
            >
              TRUSTED BY INDUSTRY LEADERS
            </motion.p>

            {/* Heading */}
            <motion.h2
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: 0.1 }}
              className="text-center font-[family-name:var(--font-playfair)] text-[36px] text-white mb-16"
            >
              Powering Remote Hiring for Companies Worldwide
            </motion.h2>
          </div>

          {/* Ticker with edge masks */}
          <div
            className="relative"
            style={{
              maskImage:
                "linear-gradient(to right, transparent, black 15%, black 85%, transparent)",
              WebkitMaskImage:
                "linear-gradient(to right, transparent, black 15%, black 85%, transparent)",
            }}
          >
            <div className="flex animate-ticker hover:[animation-play-state:paused]">
              {tickerItems.map((company, index) => (
                <div
                  key={`${company}-${index}`}
                  className="flex-shrink-0 mx-3 px-5 py-2 border border-white/[0.1] rounded-full text-sm font-[family-name:var(--font-dm-sans)] text-[#a0a0a0] hover:border-white/[0.3] hover:text-white transition-colors duration-300"
                >
                  {company}
                </div>
              ))}
            </div>
          </div>

          {/* CSS for ticker animation */}
          <style jsx>{`
            @keyframes ticker {
              0% {
                transform: translateX(0);
              }
              100% {
                transform: translateX(-50%);
              }
            }
            .animate-ticker {
              animation: ticker 30s linear infinite;
              width: max-content;
            }
          `}</style>
        </section>
      );
    }
    ```

    2. Update page.tsx to add TrustedBySection:
    ```tsx
    import { TrustedBySection } from "@/components/home/TrustedBySection";
    
    // After ShowcaseSection:
    <TrustedBySection />
    ```
  </action>
  <verify>
    <automated>grep -n "TrustedBySection" src/app/page.tsx && grep -n "animate-ticker" src/components/home/TrustedBySection.tsx && grep -n "maskImage" src/components/home/TrustedBySection.tsx</automated>
  </verify>
  <done>TrustedBySection renders label, heading, and infinite-scrolling company pills with edge fade masks and hover pause.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→DOM | Purely presentational |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | CSS ticker animation | accept | Pure CSS transform, GPU accelerated |
</threat_model>

<verification>
- [ ] Section label: "TRUSTED BY INDUSTRY LEADERS", JetBrains Mono, 11px, #555555
- [ ] Heading: Playfair Display, 36px, white, centered
- [ ] 12 company pills in ticker
- [ ] Pills scroll left continuously
- [ ] Two sets of pills for seamless loop
- [ ] Animation duration: 30s linear infinite
- [ ] Hover on ticker pauses animation
- [ ] Edge fade masks on left and right
- [ ] Pill hover: border brightens, text turns white
- [ ] Background glow at center
- [ ] Section fades up on scroll
- [ ] No console errors
</verification>

<success_criteria>
- Label: "TRUSTED BY INDUSTRY LEADERS", JetBrains Mono, 11px, uppercase, tracking 0.3em, #555555
- Heading: "Powering Remote Hiring for Companies Worldwide", Playfair Display, 36px, white
- 12 company pills: border, rounded-full, px-5 py-2, DM Sans, 14px, #a0a0a0
- CSS animation: translateX(0) → translateX(-50%), 30s linear infinite
- Duplicate items for seamless loop
- Hover: animation-play-state: paused
- Edge masks: linear-gradient(to right, transparent, black 15%, black 85%, transparent)
- Pill hover: border-white/[0.3], text-white
- Background radial gradient glow at center
- TrustedBySection rendered in page.tsx after ShowcaseSection
</success_criteria>

<output>
After completion, create `.planning/phases/07-trustedby/07-01-SUMMARY.md`
</output>
