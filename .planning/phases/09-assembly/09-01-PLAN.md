---
phase: 09-assembly
plan: 01
type: execute
wave: 3
depends_on: ["02-01", "03-01", "04-01", "05-01", "06-01", "07-01", "08-01"]
files_modified: [
  "src/app/page.tsx",
  "src/app/layout.tsx",
  "src/app/globals.css"
]
autonomous: true
requirements: [ASM-01, ASM-02]

must_haves:
  truths:
    - "All sections are assembled in correct order in page.tsx"
    - "Smooth scroll behavior is enabled globally"
    - "Page has scroll progress indicator"
    - "SEO metadata is complete in layout.tsx"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Complete home page with all sections"
      exports: ["default"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with SEO, fonts, global components"
      exports: ["metadata", "default"]
    - path: "src/app/globals.css"
      provides: "Global styles and CSS variables"
      exports: []
  key_links:
    - from: "page.tsx"
      to: "all section components"
      via: "imports and renders in order"
    - from: "layout.tsx"
      to: "Navbar, CustomCursor, Footer"
      via: "imports and renders globally"
---

<objective>
Assemble all sections into the home page, configure global features (smooth scroll, SEO, page transitions), and ensure proper integration.

Purpose: Complete the page structure and global experience.
Output: Fully assembled page.tsx with all sections in order, complete SEO, and global behaviors.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/phases/01-setup/01-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

From prior plans, page.tsx currently has:
- ScrollProgress
- HeroSection
- StatsBar
- ServicesSection
- ShowcaseSection
- TrustedBySection
- CTABanner

Need to add: Footer (already in layout.tsx)
Need to configure: smooth scroll, SEO metadata, page transitions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize page.tsx assembly</name>
  <files>src/app/page.tsx</files>
  <action>
    Update src/app/page.tsx to include all sections with proper ordering and page transition wrapper:
    ```tsx
    "use client";

    import { motion } from "framer-motion";
    import { ScrollProgress } from "@/components/shared/ScrollProgress";
    import { HeroSection } from "@/components/home/HeroSection";
    import { StatsBar } from "@/components/home/StatsBar";
    import { ServicesSection } from "@/components/home/ServicesSection";
    import { ShowcaseSection } from "@/components/home/ShowcaseSection";
    import { TrustedBySection } from "@/components/home/TrustedBySection";
    import { CTABanner } from "@/components/home/CTABanner";

    export default function Home() {
      return (
        <motion.main
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ duration: 0.4 }}
          className="min-h-screen bg-black"
        >
          <ScrollProgress />
          <HeroSection />
          <StatsBar />
          <ServicesSection />
          <ShowcaseSection />
          <TrustedBySection />
          <CTABanner />
        </motion.main>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "HeroSection" src/app/page.tsx && grep -n "CTABanner" src/app/page.tsx && grep -n "motion.main" src/app/page.tsx && grep -n "ScrollProgress" src/app/page.tsx</automated>
  </verify>
  <done>page.tsx imports and renders all sections in correct order with page fade-in transition.</done>
</task>

<task type="auto">
  <name>Task 2: Configure global styles and SEO</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <action>
    1. Update src/app/globals.css to ensure smooth scroll and base styles:
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    :root {
      --bg-primary: #000000;
      --bg-secondary: #0a0a0a;
      --bg-tertiary: #111111;
      --text-primary: #ffffff;
      --text-secondary: #a0a0a0;
      --text-muted: #555555;
      --accent: #ffffff;
      --accent-glow: rgba(255,255,255,0.08);
      --border-subtle: rgba(255,255,255,0.08);
      --gradient-hero: radial-gradient(ellipse at 50% 0%, rgba(255,255,255,0.12) 0%, transparent 60%);
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      background-color: var(--bg-primary);
      color: var(--text-primary);
      font-family: var(--font-dm-sans), sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    ::selection {
      background: rgba(255,255,255,0.2);
      color: #ffffff;
    }

    /* Hide scrollbar for cleaner look */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: #000000;
    }

    ::-webkit-scrollbar-thumb {
      background: #333333;
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #555555;
    }
    ```

    2. Update src/app/layout.tsx with complete SEO metadata:
    ```tsx
    import type { Metadata } from "next";
    import { Playfair_Display, DM_Sans, JetBrains_Mono } from "next/font/google";
    import { CustomCursor } from "@/components/shared/CustomCursor";
    import { Navbar } from "@/components/shared/Navbar";
    import { Footer } from "@/components/shared/Footer";
    import "./globals.css";

    const playfair = Playfair_Display({
      subsets: ["latin"],
      weight: ["400", "700", "900"],
      variable: "--font-playfair",
      display: "swap",
    });

    const dmSans = DM_Sans({
      subsets: ["latin"],
      weight: ["300", "400", "500"],
      variable: "--font-dm-sans",
      display: "swap",
    });

    const jetbrains = JetBrains_Mono({
      subsets: ["latin"],
      weight: ["400"],
      variable: "--font-jetbrains",
      display: "swap",
    });

    export const metadata: Metadata = {
      title: "Hiretivo — Skill-Matched Remote Jobs",
      description:
        "Find remote jobs that match your actual skills. Hiretivo's skill-overlap engine surfaces only roles where your profile genuinely qualifies.",
      keywords: [
        "remote jobs",
        "skill matching",
        "job portal",
        "hiring",
        "remote work",
        "skills-based hiring",
      ],
      authors: [{ name: "Hiretivo" }],
      openGraph: {
        title: "Hiretivo — Skill-Matched Remote Jobs",
        description:
          "Find remote jobs that match your actual skills. Hiretivo's skill-overlap engine surfaces only roles where your profile genuinely qualifies.",
        type: "website",
        locale: "en_US",
        siteName: "Hiretivo",
      },
      twitter: {
        card: "summary_large_image",
        title: "Hiretivo — Skill-Matched Remote Jobs",
        description:
          "Find remote jobs that match your actual skills. Hiretivo's skill-overlap engine surfaces only roles where your profile genuinely qualifies.",
      },
      robots: {
        index: true,
        follow: true,
      },
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html
          lang="en"
          className={`${playfair.variable} ${dmSans.variable} ${jetbrains.variable}`}
        >
          <body className="bg-black text-white">
            <CustomCursor />
            <Navbar />
            {children}
            <Footer />
          </body>
        </html>
      );
    }
    ```
  </action>
  <verify>
    <automated>grep -n "openGraph" src/app/layout.tsx && grep -n "scroll-behavior" src/app/globals.css && grep -n "Navbar" src/app/layout.tsx && grep -n "Footer" src/app/layout.tsx</automated>
  </verify>
  <done>layout.tsx has complete SEO metadata (title, description, OG, Twitter, robots), globals.css has smooth scroll and custom scrollbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→build | SEO metadata is static at build time |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | SEO metadata | accept | Public marketing content |
</threat_model>

<verification>
- [ ] page.tsx renders all sections in order
- [ ] Page has fade-in transition (opacity 0→1)
- [ ] ScrollProgress at top of page
- [ ] layout.tsx has complete metadata object
- [ ] OG tags present (title, description, type, locale, siteName)
- [ ] Twitter card metadata present
- [ ] Robots meta set to index, follow
- [ ] HTML has scroll-behavior: smooth
- [ ] Custom scrollbar styling applied
- [ ] All imports resolve without errors
- [ ] Build succeeds
</verification>

<success_criteria>
- page.tsx: ScrollProgress → HeroSection → StatsBar → ServicesSection → ShowcaseSection → TrustedBySection → CTABanner
- Page wrapped in motion.main with fade-in transition
- layout.tsx: Complete metadata with title, description, keywords, authors
- OpenGraph: title, description, type, locale, siteName
- Twitter card: summary_large_image, title, description
- Robots: index: true, follow: true
- globals.css: scroll-behavior: smooth, custom scrollbar, selection styling
- CustomCursor, Navbar, Footer rendered in layout.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/09-assembly/09-01-SUMMARY.md`
</output>
