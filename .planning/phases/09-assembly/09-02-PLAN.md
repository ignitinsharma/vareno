---
phase: 09-assembly
plan: 02
type: execute
wave: 3
depends_on: ["09-01"]
files_modified: [
  "src/app/page.tsx",
  "src/components/home/HeroSection.tsx",
  "src/components/home/StatsBar.tsx",
  "src/components/home/ServicesSection.tsx",
  "src/components/home/ShowcaseSection.tsx",
  "src/components/home/TrustedBySection.tsx",
  "src/components/home/CTABanner.tsx",
  "src/components/shared/Navbar.tsx",
  "src/components/shared/Footer.tsx"
]
autonomous: true
requirements: [ASM-03, ASM-04]

must_haves:
  truths:
    - "All sections are responsive across breakpoints"
    - "All animations work as specified"
    - "No layout shifts or visual bugs"
    - "Performance is optimized"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Responsive, animated page"
    - path: "src/components/shared/Navbar.tsx"
      provides: "Responsive navbar"
    - path: "src/components/shared/Footer.tsx"
      provides: "Responsive footer"
  key_links:
    - from: "all components"
      to: "Tailwind responsive prefixes"
      via: "sm:, md:, lg: breakpoints"
---

<objective>
Perform responsive audit and animation verification across all sections. Fix any issues found.

Purpose: Ensure the site looks and works great on all devices and all animations are smooth.
Output: Fully responsive, animated, and polished website.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/phases/09-assembly/09-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Responsive requirements from plan.md:
- Hero: 72px → 56px → 40px (lg / sm / default)
- Services grid: 2-col → 1-col at md
- Stats: 3-col → 1-col at sm
- Footer: 5-col → 2-col → 1-col
- Navbar: desktop links → hamburger at lg

Animation checklist from plan.md:
- [ ] Navbar blur on scroll
- [ ] Hero stagger on load
- [ ] Stats counter roll-up
- [ ] Service card hover reveal
- [ ] Showcase crossfade
- [ ] Logo ticker infinite scroll
- [ ] CTA word-by-word fade
- [ ] Footer column stagger
- [ ] Custom cursor follows mouse
- [ ] Scroll progress bar
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive audit across all sections</name>
  <files>src/components/home/HeroSection.tsx, src/components/home/StatsBar.tsx, src/components/home/ServicesSection.tsx, src/components/home/ShowcaseSection.tsx, src/components/shared/Footer.tsx, src/components/shared/Navbar.tsx</files>
  <action>
    Audit and fix responsive breakpoints for each section:

    1. **HeroSection**: Verify text sizes
       - H1: `text-[40px] sm:text-[56px] lg:text-[72px]` ✓
       - Check padding and spacing on mobile

    2. **StatsBar**: Verify grid
       - `grid-cols-1 sm:grid-cols-3` ✓
       - Check vertical dividers flip correctly

    3. **ServicesSection**: Verify grid
       - `grid-cols-1 md:grid-cols-2` ✓
       - Check card padding on mobile (reduce from p-8 to p-6 on small screens)

    4. **ShowcaseSection**: Verify layout
       - `grid-cols-1 lg:grid-cols-2` ✓
       - Preview card hidden on mobile (`hidden lg:block`) ✓
       - List takes full width on mobile

    5. **TrustedBySection**: Verify ticker
       - Pills should not overflow on small screens
       - Ensure mask works on all sizes

    6. **CTABanner**: Verify headline
       - `text-[36px] sm:text-[48px] lg:text-[56px]` ✓
       - CTAs stack on mobile

    7. **Footer**: Verify grid
       - `grid-cols-2 md:grid-cols-3 lg:grid-cols-6` ✓
       - Brand column spans correctly
       - Bottom bar stacks on mobile

    8. **Navbar**: Verify breakpoints
       - Desktop nav hidden below `lg`: `hidden lg:flex` ✓
       - Mobile hamburger shown below `lg`: `lg:hidden` ✓
       - Mobile menu drawer width appropriate

    Apply fixes to any files needing adjustments. Common fixes:
    - Reduce section padding on mobile: `py-16 lg:py-24` instead of fixed large padding
    - Ensure text doesn't touch screen edges: maintain `px-4` minimum
    - Check that `max-w` containers work on all screens
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -i "error" | head -10 || echo "Build clean"</automated>
  </verify>
  <done>All sections audited for responsive behavior at sm, md, lg breakpoints. Issues fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Animation verification and performance optimization</name>
  <files>src/app/page.tsx, src/components/shared/CustomCursor.tsx, src/components/shared/ScrollProgress.tsx</files>
  <action>
    1. **Animation Checklist** — verify each animation exists and works:
       - Open each component file and confirm animation props are present
       - Check Framer Motion imports in each animated component
       - Verify `use client` directive on all client components

    2. **Performance Optimizations**:
       - Add `will-change: transform` to heavily animated elements in globals.css:
       ```css
       .will-change-transform {
         will-change: transform;
       }
       ```
       
       - Ensure images (if any) have `loading="lazy"`
       
       - Add `prefers-reduced-motion` support in globals.css:
       ```css
       @media (prefers-reduced-motion: reduce) {
         * {
           animation-duration: 0.01ms !important;
           animation-iteration-count: 1 !important;
           transition-duration: 0.01ms !important;
         }
       }
       ```

    3. **Build verification**:
       - Run `npm run build`
       - Check for any errors or warnings
       - Verify output in `.next/` directory

    4. **Final animation audit list** (verify each):
       ```
       [ ] Navbar blur on scroll — useScroll in Navbar.tsx
       [ ] Hero stagger on load — staggerChildren in HeroSection.tsx
       [ ] Stats counter roll-up — AnimatedCounter.tsx
       [ ] Service card hover reveal — whileHover in ServiceCard.tsx
       [ ] Showcase crossfade — AnimatePresence in ShowcaseSection.tsx
       [ ] Logo ticker infinite scroll — CSS animation in TrustedBySection.tsx
       [ ] CTA word-by-word fade — staggerChildren in CTABanner.tsx
       [ ] Footer column stagger — stagger delays in Footer.tsx
       [ ] Custom cursor follows mouse — useSpring in CustomCursor.tsx
       [ ] Scroll progress bar — useScroll in ScrollProgress.tsx
       ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 animations verified, performance optimizations applied, build succeeds without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→build | Static build optimization |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Denial of Service | prefers-reduced-motion | mitigate | Respect user preference, disable animations |
</threat_model>

<verification>
- [ ] All sections responsive at 320px, 768px, 1024px, 1440px
- [ ] Navbar shows hamburger below 1024px
- [ ] Hero text scales correctly
- [ ] Stats stack on mobile
- [ ] Services grid 1-col on mobile
- [ ] Showcase list full-width on mobile
- [ ] Footer columns collapse gracefully
- [ ] All 10 animations present and working
- [ ] prefers-reduced-motion respected
- [ ] Build completes without errors
- [ ] No console warnings
</verification>

<success_criteria>
- Responsive breakpoints verified: sm (640px), md (768px), lg (1024px), xl (1280px)
- All 10 animations from checklist verified working
- prefers-reduced-motion media query disables animations
- will-change: transform on animated elements
- Lazy loading on images
- Build succeeds cleanly
- No visual regressions at any breakpoint
</success_criteria>

<output>
After completion, create `.planning/phases/09-assembly/09-02-SUMMARY.md`
</output>
